pre-push:
  parallel: true
  commands:
    build:
      run: pnpm build

pre-commit:
  parallel: true
  commands:
    check:
      run: pnpm check && git add {staged_files}

commit-msg:
  commands:
    commitlint:
      run: npx commitlint -e
